import React from "react"

import Header from "../components/header"
import Footer from "../components/footer"

const BlogPage = () => {
    return (
        <div>
            <Header />
            <h1>Blog</h1>
            <p>Posts will show up here later on.</p>
            <Footer />
        </div>
    )
}

export default BlogPage

//Goal: Create an about page and a contact page

// 1. creat an ABOUT page. include a page title h1 and bio p
// 2. create a CONTACT page. include a page title and contact details
// 3. test your work. visit in browsers and visible showing up
